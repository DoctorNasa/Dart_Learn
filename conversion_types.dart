// you can converte type of string into other type

main(
  // string ->> int
  var one = int.parse('1');
  assert(one == '1');
 // int ->> string
  String oneAsString = 1.toString();
  assert(oneAsString == '1');
   // string ->> double
   var OnePointone = double.parse('1.1');
   assert(onePoinone == 1.1)
    // duble ->> string
  String piAsString = 2.3421594.toStringAsFixed(2);
  assert(piAsString == '2.34')

)